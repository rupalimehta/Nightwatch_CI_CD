#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nightwatch-html-reporter/lib/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../nightwatch-html-reporter/lib/cli.js" "$@"
  ret=$?
fi
exit $ret
